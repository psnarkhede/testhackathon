openapi: 3.0.0
info:
  title: API Documentation
  description: API generated from schema version f5b428344dc30bebae54d94c8acad373a1af84e6
  version: 1.0.0
servers:
  - url: http://localhost:3000
    description: Development server
paths:
  /:
    get:
      summary: Root endpoint
      description: Retrieves a basic API response.
      operationId: AppController_Get
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: object
  /bookings:
    get:
      summary: Get bookings
      description: Retrieves booking information for a user.
      operationId: AppController_GetBookings
      parameters:
        - name: mobileNumber
          in: query
          description: The user's mobile number.
          required: true
          schema:
            type: string
        - name: uuid
          in: query
          description: A unique identifier for the user or request.
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successfully retrieved bookings.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BookingResponseDto'
        '400':
          description: Bad Request - Missing required query parameters.
components:
  schemas:
    BookingResponseDto:
      type: object
      properties:
        vehicle:
          type: string
          description: The vehicle associated with the booking.
        uuid:
          type: string
          description: The unique identifier for the booking request.
        amount:
          type: number
          format: float
          description: The amount for the booking. This field is optional.
      required:
        - vehicle
        - uuid